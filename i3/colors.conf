
# Soft pastel pink-based colors
set $bg          "#FDE2E4"   
set $fg          "#FFF1F3"   
set $accent      "#FF99C8"   
set $inactive    "#FEC8D8"   

# Window colors
client.focused          $accent $bg $fg $accent $bg
client.unfocused        $inactive $bg $fg $inactive $bg
client.focused_inactive $inactive $bg $fg $inactive $bg

